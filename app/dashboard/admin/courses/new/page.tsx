import { all } from "@/db/lms"; import { LmsShell, Notice } from "../../../components"; import { requireAdminPermission } from "../../../auth"; type User={id:string;name:string};
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const profile=await requireAdminPermission("manage_courses","/dashboard/admin/courses/new");const tutors=await all<User>("SELECT id,name FROM users WHERE role='tutor' AND status!='suspended' ORDER BY name");const students=await all<User>("SELECT id,name FROM users WHERE role='student' AND status!='suspended' ORDER BY name");return <LmsShell profile={profile} activeRole="admin" activePath="/dashboard/admin/courses" eyebrow="Learning programmes" title="Create a course" subtitle="Set the learning goal, then assign a tutor and optionally enrol a learner." actions={<a className="module-link secondary" href="/dashboard/admin/courses">← All courses</a>}><Notice params={await searchParams}/><div className="form-page"><section className="form-card"><form action="/api/lms" method="post"><input type="hidden" name="action" value="create-course"/><label>Course title<input name="title" required placeholder="Physics Foundations"/></label><div className="form-row"><label>Subject<input name="subject" required placeholder="Physics"/></label><label>Level<input name="level" required placeholder="Grade 9"/></label></div><label>Assigned tutor<select name="tutorId" required><option value="">Choose a tutor</option>{tutors.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><label>Enrol learner<select name="studentId"><option value="">Assign later</option>{students.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label>Description<textarea name="description" required rows={5} placeholder="Learning goals and 1:1 programme focus"/></label><button>Create course →</button></form></section><aside className="help-card"><h3>Course setup</h3><p>After creation the tutor can schedule live classes and publish learning work.</p></aside></div></LmsShell>}
