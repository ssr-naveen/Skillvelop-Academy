import { all, first } from "@/db/lms";
import { LmsShell, Status, formatDate } from "../../components";
import { requireAdminPermission } from "../../auth";
type Row={id:string;title:string;starts_at:string;duration_minutes:number;course:string;tutor:string;student:string|null;status:string;student_rating:number|null};
export const dynamic="force-dynamic";
export default async function Page(){const profile=await requireAdminPermission("manage_classes","/dashboard/admin/classes"),timezone=(await first<{timezone:string}>("SELECT timezone FROM user_profiles WHERE user_id=?",profile.id))?.timezone??"Asia/Kolkata";const rows=await all<Row>(`SELECT l.id,l.title,l.starts_at,l.duration_minutes,l.status,l.student_rating,c.title course,t.name tutor,s.name student FROM live_classes l JOIN courses c ON c.id=l.course_id JOIN users t ON t.id=l.tutor_id LEFT JOIN users s ON s.id=l.student_id ORDER BY l.starts_at DESC`);return <LmsShell profile={profile} activeRole="admin" activePath="/dashboard/admin/classes" eyebrow="Live teaching" title="Class operations" subtitle={`The academy schedule is displayed in ${timezone}. Only student-confirmed sessions count as delivered.`}><section className="panel"><div className="class-list">{rows.map(r=><article className="class-row" key={r.id}><div className="class-time"><strong>{new Intl.DateTimeFormat('en-IN',{timeZone:timezone,hour:'numeric',minute:'2-digit'}).format(new Date(r.starts_at))}</strong><span>{new Intl.DateTimeFormat('en-IN',{timeZone:timezone,day:'numeric',month:'short'}).format(new Date(r.starts_at))}</span></div><div className="class-accent"/><div className="class-detail"><span>{r.course}</span><h3>{r.title}</h3><p>{r.tutor} with {r.student??'course learners'} · {formatDate(r.starts_at,timezone)} · {r.duration_minutes} min</p></div><Status tone={r.status==='completed'?'green':r.status==='awaiting_confirmation'?'amber':'blue'}>{r.status.replaceAll('_',' ')}{r.student_rating?` · ${r.student_rating}/5`:''}</Status></article>)}</div></section></LmsShell>}
