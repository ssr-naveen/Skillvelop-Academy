import { all } from "@/db/lms";
import { MathFormField } from "../../../MathComposer";
import { DateTimePicker12 } from "@/app/components/DateTimePicker12";
import { LmsShell, Notice } from "../../../components";
import { requireRole } from "../../../auth";
type Course={id:string;title:string};
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const profile=await requireRole("tutor","/dashboard/tutor/work/new");const tutorId=profile.role==='admin'?'usr_demo_tutor':profile.id;const courses=await all<Course>("SELECT id,title FROM courses WHERE tutor_id=? AND status='active' ORDER BY title",tutorId);return <LmsShell profile={profile} activeRole="tutor" activePath="/dashboard/tutor/work" eyebrow="Learning activities" title="Assign new work" subtitle="Create maths-ready homework, class work, assignments and assessments for enrolled students." actions={<a className="module-link secondary" href="/dashboard/tutor/work">← Assigned work</a>}><Notice params={await searchParams}/><div className="form-page"><section className="form-card"><form action="/api/lms" method="post"><input type="hidden" name="action" value="create-activity"/><label>Course<select name="courseId" required><option value="">Choose course</option>{courses.map(c=><option key={c.id} value={c.id}>{c.title}</option>)}</select></label><label>Work type<select name="type"><option value="homework">Homework</option><option value="quiz">Quiz</option><option value="assessment">Assessment</option><option value="assignment">Assignment</option><option value="classwork">Class work</option></select></label><MathFormField name="title" label="Title" required compact placeholder="What should the learner complete?"/><MathFormField name="instructions" label="Instructions and success criteria" multiline required placeholder="Add text, formulae, equations, symbols and clear success criteria…"/><div className="form-row schedule-row"><DateTimePicker12 name="dueAt" label="Due date" required/><label>Points<input name="points" type="number" min="1" max="1000" defaultValue="100"/></label></div><label className="check-row"><input type="checkbox" name="isUnlocked"/> Unlock for students immediately</label><button>Publish to learner →</button></form></section><aside className="help-card"><h3>Maths-ready activities</h3><p>Use the advanced equation palette for fractions, roots, powers, algebra, geometry, sets, calculus and matrices. Students receive the same editor for their response.</p></aside></div></LmsShell>}

