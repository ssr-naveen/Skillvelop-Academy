import {all} from "@/db/lms";
import {MathFormField} from "../../MathComposer";
import{LmsShell,Notice,Status}from"../../components";
import{requireRole}from"../../auth";
import DeleteButton from "../../DeleteButton";
type C={id:string;title:string;subject:string;level:string;description:string;cover_image_url:string;is_unlocked:number;students:number;chapters:number;lessons:number;imported:number};type S={id:string;name:string};export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await requireRole("tutor","/dashboard/tutor/courses"),t=p.role==='admin'?'usr_demo_tutor':p.id;const courses=await all<C>(`SELECT c.id,c.title,c.subject,c.level,c.description,c.cover_image_url,c.is_unlocked,COUNT(DISTINCT e.id) students,COUNT(DISTINCT ch.id) chapters,COUNT(DISTINCT l.id) lessons,CASE WHEN ci.id IS NULL THEN 0 ELSE 1 END imported FROM courses c LEFT JOIN enrollments e ON e.course_id=c.id LEFT JOIN chapters ch ON ch.course_id=c.id LEFT JOIN lessons l ON l.course_id=c.id LEFT JOIN curriculum_imports ci ON ci.course_id=c.id WHERE c.tutor_id=? AND c.status='active' GROUP BY c.id ORDER BY c.created_at DESC`,t);const students=await all<S>("SELECT id,name FROM users WHERE role='student' AND status!='suspended' ORDER BY name");return <LmsShell profile={p} activeRole="tutor" activePath="/dashboard/tutor/courses" eyebrow="Teaching programmes" title="My courses" subtitle="Create illustrated, maths-ready course cards, enrol students and control when each course becomes available." actions={<a className="module-link" href="/dashboard/tutor/curriculum">Build chapters & lessons →</a>}><Notice params={await searchParams}/><div className="course-management-layout"><section className="tutor-course-grid">{courses.map(c=><article className="tutor-course-card" key={c.id}><div className="tutor-course-cover" style={c.cover_image_url?{backgroundImage:`linear-gradient(0deg,rgba(12,31,38,.72),rgba(12,31,38,.05)),url(${c.cover_image_url})`}:undefined}><Status tone={c.is_unlocked?'green':'amber'}>{c.is_unlocked?'Student access on':'Locked'}</Status><span>{c.subject}</span></div><div className="tutor-course-body"><small>{c.level}{c.imported?' · Academy curriculum':''}</small><h2>{c.title}</h2><p>{c.description}</p><div className="course-counts"><span><b>{c.chapters}</b> Chapters</span><span><b>{c.lessons}</b> Lessons</span><span><b>{c.students}</b> Students</span></div><form className="unlock-form" action="/api/lms" method="post"><input type="hidden" name="action" value="set-content-unlock"/><input type="hidden" name="entity" value="course"/><input type="hidden" name="id" value={c.id}/><input type="hidden" name="next" value={c.is_unlocked?'0':'1'}/><input type="hidden" name="returnTo" value="/dashboard/tutor/courses?updated=unlock"/><button>{c.is_unlocked?'Lock course':'Unlock course for students'}</button></form><form className="course-enrol-form" action="/api/lms" method="post"><input type="hidden" name="action" value="enrol-course-student"/><input type="hidden" name="courseId" value={c.id}/><select name="studentId" required><option value="">Choose student...</option>{students.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><button>Enrol</button></form>{c.imported?<p className="protected-content">Imported academy courses are protected from tutor deletion.</p>:<DeleteButton action="delete-course" id={c.id} name="courseId" label={`course “${c.title}”`}/>}</div></article>)}</section><aside className="form-card sticky-builder"><form action="/api/lms" method="post"><input type="hidden" name="action" value="create-tutor-course"/><h2>Create a course</h2><p>Courses begin locked so you can build their chapters before students enter.</p><MathFormField name="title" label="Course title" required compact placeholder="Grade 5 Mathematics"/><div className="form-row"><label>Subject<input name="subject" required placeholder="Mathematics"/></label><label>Level<input name="level" required placeholder="Grade 5"/></label></div><label>Course card image URL<input name="coverImageUrl" type="url" placeholder="https://…/course-cover.jpg"/></label><MathFormField name="description" label="Course description and outcomes" multiline required placeholder="What students will learn and achieve, including equations or symbols…"/><button>Create locked course →</button></form></aside></div></LmsShell>}
