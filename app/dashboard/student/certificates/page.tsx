import { all } from "@/db/lms";
import { LmsShell, Status } from "../../components";
import { requireRole } from "../../auth";
type Certificate={id:string;certificate_code:string;issued_at:string;course:string;subject:string;tutor:string};
export const dynamic="force-dynamic";
export default async function Page(){const profile=await requireRole("student","/dashboard/student/certificates");const studentId=profile.role==='admin'?'usr_demo_student':profile.id;const certificates=await all<Certificate>(`SELECT ce.id,ce.certificate_code,ce.issued_at,c.title course,c.subject,u.name tutor FROM certificates ce JOIN courses c ON c.id=ce.course_id JOIN users u ON u.id=c.tutor_id WHERE ce.student_id=? ORDER BY ce.issued_at DESC`,studentId);return <LmsShell profile={profile} activeRole="student" activePath="/dashboard/student/certificates" eyebrow="Celebrate your progress" title="My certificates" subtitle="Course completion certificates are generated automatically when every lesson is complete."><div className="certificate-grid">{certificates.map(item=><a className="certificate-card" href={`/dashboard/student/certificates/${item.id}`} key={item.id}><span>★</span><small>SKILVELOP CERTIFICATE</small><h2>{item.course}</h2><p>{item.subject} · Tutor {item.tutor}</p><footer><Status>Completed</Status><b>View & print →</b></footer></a>)}{certificates.length===0&&<section className="panel empty-state"><span>♛</span><strong>Your first certificate is waiting</strong><p>Complete every lesson in a course and it will appear here automatically.</p><a className="primary-action" href="/dashboard/student/curriculum">Continue learning →</a></section>}</div></LmsShell>}
