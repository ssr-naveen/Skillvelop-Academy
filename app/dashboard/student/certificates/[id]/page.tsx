import { first } from "@/db/lms";
import { requireRole } from "../../../auth";
import { notFound } from "next/navigation";
import PrintCertificate from "./PrintCertificate";
type Certificate={certificate_code:string;issued_at:string;course:string;subject:string;student:string;tutor:string};
export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{id:string}>}){const profile=await requireRole("student","/dashboard/student/certificates");const studentId=profile.role==='admin'?'usr_demo_student':profile.id;const {id}=await params;const certificate=await first<Certificate>(`SELECT ce.certificate_code,ce.issued_at,c.title course,c.subject,s.name student,t.name tutor FROM certificates ce JOIN courses c ON c.id=ce.course_id JOIN users s ON s.id=ce.student_id JOIN users t ON t.id=c.tutor_id WHERE ce.id=? AND ce.student_id=?`,id,studentId);if(!certificate)notFound();return <main className="certificate-page"><div className="certificate-actions"><a href="/dashboard/student/certificates">← Back to certificates</a><PrintCertificate/></div><article className="certificate-document"><div className="certificate-seal">S</div><span className="certificate-brand">SKILVELOP ACADEMY</span><p className="certificate-overline">CERTIFICATE OF COURSE COMPLETION</p><h1>Certificate<br/>of Achievement</h1><p className="certificate-presented">This certificate is proudly presented to</p><h2>{certificate.student}</h2><p className="certificate-copy">for successfully completing the full learning programme</p><h3>{certificate.course}</h3><p>{certificate.subject}</p><footer><div><b>{certificate.tutor}</b><span>Course tutor</span></div><div className="certificate-medal">★</div><div><b>{new Date(certificate.issued_at).toLocaleDateString('en-IN',{day:'numeric',month:'long',year:'numeric'})}</b><span>Date awarded</span></div></footer><small>Credential ID: {certificate.certificate_code}</small></article></main>}
